{% extends 'base.html.twig' %}

{% block title %}Ajouter une recette{% endblock %}

{% block body %}
    <section class="container">
        <h1>Ajouter une nouvelle recette</h1>
        {{ form_start(form, {'attr': {'id': 'formRecette'}}) }}

            {{ form_widget(form) }}
         <button >Ajouter des ingrédients</button>
        {{form_end(form)}}
    </section>
    {# <section class="container"> #}
      {# <button type="button" class="btn buttonA" data-bs-toggle="modal" data-bs-target="#myModal" id="subBtn">
        Passer aux étapes suivantes
        </button>

        <!-- Modal -->

        <div class="modal fade" id="myModal" tabindex="-1" aria-labelledby="myModalLabel" aria-hidden="true">
            <div class="modal-dialog modal-lg">
                <div class="modal-content">
                    <div class="modal-header">
                        <h5 class="modal-title" id="myModalLabel">Les informations de cette recette</h5>
                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                    </div>
                    <div class="modal-body">
                    <!-- Nav tabs -->
                        <ul class="nav nav-tabs" id="myTab" role="tablist">
                            <li class="nav-item" role="presentation">
                                <a class="nav-link active" id="ingredients-tab" data-bs-toggle="tab" href="#ingredients" role="tab" aria-controls="ingredients" aria-selected="true">Ingrédients</a>
                            </li>
                            <li class="nav-item" role="presentation">
                                <a class="nav-link" id="ustensiles-tab" data-bs-toggle="tab" href="#ustensiles" role="tab" aria-controls="ustensiles" aria-selected="false">Ustensiles</a>
                            </li>
                            <li class="nav-item" role="presentation">
                                <a class="nav-link" id="etapes-tab" data-bs-toggle="tab" href="#etapes" role="tab" aria-controls="etapes" aria-selected="false">Étapes</a>
                            </li>
                            <li class="nav-item" role="presentation">
                                <a class="nav-link" id="tags-tab" data-bs-toggle="tab" href="#tags" role="tab" aria-controls="tags" aria-selected="false">Mots clés</a>
                            </li>
                        </ul>
                        <!-- Tab content -->
                        <div class="tab-content mt-3" id="myTabContent">
                            <div class="tab-pane fade show active" id="ingredients" role="tabpanel" aria-labelledby="ingredients-tab">
                                <h5>Saisissez les ingrédients de la recette</h5>
                                <p>Ceci est le contenu de l'onglet 1.</p>
                                    <p> {{ form_start(form1) }}
                                            {{ form_widget(form1) }}
                                            <button type="submit" class="btn btn-primary">Valider</button>
                                        {{ form_end(form1) }}</p> 
                            </div>
                            <div class="tab-pane fade" id="ustensiles" role="tabpanel" aria-labelledby="ustensiles-tab">
                                <h5>Notez les ustensiles nécessaires pour réaliser la recette</h5>
                                <p>Ceci est le contenu de l'onglet 2.</p>
                                    <p> {{ form_start(form2) }}
                                            {{ form_widget(form2) }}
                                            <button type="submit" class="btn btn-primary">Valider</button>
                                        {{ form_end(form2) }}</p>
                            </div>
                            <div class="tab-pane fade" id="etapes" role="tabpanel" aria-labelledby="etapes-tab">
                                <h5>Notez les différentes étapes pour réaliser la recette</h5>
                                <p>Ceci est le contenu de l'onglet 3.</p>
                                <p> {{ form_start(form3) }}
                                            {{ form_widget(form3) }}
                                            <button type="submit" class="btn btn-primary">Valider</button>
                                        {{ form_end(form3) }}</p>
                            </div>
                            <div class="tab-pane fade" id="tags" role="tabpanel" aria-labelledby="tags-tab">
                                <h5>Quels sont les mots clés de cette recette</h5>
                                <p>Ceci est le contenu de l'onglet 4.</p>
                                <p> {{ form_start(form4) }}
                                            {{ form_widget(form4) }}
                                            <button type="submit" class="btn btn-primary">Valider</button>
                                        {{ form_end(form4) }}</p>
                            </div>
                        </div>
                    </div>
                        <div class="modal-footer">
                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Fermer</button>
                            <button type="button" class="btn btn-primary">Sauvegarder les modifications</button>
                        </div>
                </div>
            </div>
        </div>

    </section> #}
                   
{% endblock %}

